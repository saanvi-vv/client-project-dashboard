{
  "version": 2,
  "installCommand": "cd api && npm install && npx prisma generate --schema=../server/prisma/schema.prisma && cd ../client && npm install",
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
